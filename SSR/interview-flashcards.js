/* =============================================================================
 * SSR & ARCHITECTURE — FLASHCARDS (Tier 8) · Full answers: THEORY.md
 * =============================================================================
 *  SSR vs CSR       → HTML on server vs JS renders in browser
 *  SSR pros         → FCP, SEO, social meta
 *  SSR cons         → server load, hydration complexity
 *  Hydration        → attach React to server HTML on client
 *  Next.js          → React framework: SSR, SSG, ISR, App Router
 *  getServerSideProps → every request (dynamic)
 *  getStaticProps   → build time + optional revalidate (ISR)
 *  Choose SSR       → SEO + fresh/personalized initial HTML
 * ============================================================================= */
