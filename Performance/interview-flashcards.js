/* =============================================================================
 * PERFORMANCE — FLASHCARDS (Tier 6) · Full answers: THEORY.md
 * =============================================================================
 *  Slow React app   → Profiler, memo, split bundle, virtualize lists
 *  useMemo when     → expensive calc, stable refs for memo children
 *  avoid useMemo    → cheap ops, deps change every render
 *  Code splitting   → dynamic import / React.lazy per route
 *  Tree shaking     → remove unused ESM exports at build
 *  Lazy loading     → load code/assets when needed
 *  Virtualization   → render only visible list rows
 *  Bundle size      → analyze, split, shake, replace heavy libs
 *  Lighthouse       → automated perf/a11y/SEO audit
 *  Debug perf       → Performance tab, Profiler, Network, Web Vitals
 * ============================================================================= */
