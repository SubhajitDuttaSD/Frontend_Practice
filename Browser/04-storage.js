/* Storage — Theory: THEORY.md §4
 * localStorage  — persistent, all tabs same origin
 * sessionStorage — per tab, cleared on close
 * cookies        — ~4KB, sent with every HTTP request, HttpOnly for auth
 */
