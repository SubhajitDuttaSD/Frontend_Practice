/* =============================================================================
 * BROWSER — FLASHCARDS (Tier 4) · Full answers: THEORY.md
 * =============================================================================
 *  URL entered      → DNS → TCP/TLS → HTTP → parse → layout → paint
 *  DOM              → document tree API for HTML
 *  BOM              → window, location, navigator, history
 *  Bubbling         → event travels target → ancestors
 *  Capturing        → event travels document → target (use capture: true)
 *  Delegation       → one parent listener; works for dynamic children
 *  localStorage     → persistent, all tabs | sessionStorage → per tab
 *  Cookies          → sent on HTTP; HttpOnly for auth tokens
 *  CORS             → blocks cross-origin reads unless server allows
 *  Preflight        → OPTIONS before non-simple cross-origin requests
 * ============================================================================= */
