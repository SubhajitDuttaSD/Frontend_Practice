/* =============================================================================
 * CSS — FLASHCARDS (Tier 5) · Full answers: THEORY.md
 * =============================================================================
 *  Box model        → content, padding, border, margin
 *  Specificity      → inline > id > class > element
 *  Flex vs Grid     → 1D components vs 2D page layout
 *  absolute         → out of flow; relative to positioned ancestor
 *  sticky           → relative until scroll, then sticks
 *  display:none     → removed from layout | visibility:hidden → space kept
 *  inline-block     → inline flow + width/height/margin work
 *  :hover vs ::before → state vs virtual element
 *  z-index          → stacking; needs positioned element + context
 *  Layout shift     → images without size, fonts, late ads (CLS)
 * ============================================================================= */
