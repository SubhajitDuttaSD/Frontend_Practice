/* Flexbox: 1D alignment | Grid: 2D layouts
 * relative: in flow, offset from self | absolute: out of flow, positioned ancestor
 * sticky: relative until scroll threshold, then fixed in container
 */
