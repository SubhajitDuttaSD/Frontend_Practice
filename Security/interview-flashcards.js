/* =============================================================================
 * SECURITY — FLASHCARDS (Tier 7) · Full answers: THEORY.md
 * =============================================================================
 *  XSS              → inject malicious script into page
 *  Types            → Stored, Reflected, DOM-based
 *  React XSS        → JSX escapes by default
 *  dangerouslySetInnerHTML → raw HTML; sanitize with DOMPurify
 *  SQL injection    → untrusted input in SQL string
 *  Prepared stmts   → params separate from query shape
 *  CSP              → header restricting script/resource sources
 * ============================================================================= */
